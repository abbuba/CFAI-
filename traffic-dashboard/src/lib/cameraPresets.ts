import * as THREE from "three";

/** Single demo view — close to Node A traffic lights. */
export const NODE_A_CAMERA = {
  position: new THREE.Vector3(-11, 5, -5),
  target: new THREE.Vector3(-14, 1.2, -9),
  label: "Node A",
};
